name: CI

on:
  push:
<<<<<<< HEAD
    branches: [master, main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
=======
    branches: [master]
  pull_request:

jobs:
  verify:
    name: Lint, typecheck, test and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
>>>>>>> 005c357b565eaf6ff99b0cc04ff8ed07cf1d64a0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build:shared

<<<<<<< HEAD
      - name: Prisma generate
=======
      # Fresh `npm ci` has no generated Prisma client, and there is no
      # postinstall hook, so generate it before the API can be compiled.
      - name: Generate Prisma client
>>>>>>> 005c357b565eaf6ff99b0cc04ff8ed07cf1d64a0
        run: npm run prisma:generate --workspace @edt/api

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

<<<<<<< HEAD
      - name: Unit tests
        run: npm test --workspace @edt/api

      - name: Build API
        run: npm run build:api

      - name: Build Web
        run: npm run build:web
        env:
          NEXT_TELEMETRY_DISABLED: '1'
=======
      - name: Test
        run: npm run test --workspaces --if-present

      - name: Build apps
        run: npm run build
>>>>>>> 005c357b565eaf6ff99b0cc04ff8ed07cf1d64a0
